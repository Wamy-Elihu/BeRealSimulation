def capture_image():
    
        """
    Simule la capture d'une image.
    Retourne une chaîne représentant les données de l'image.
    """
    
    try:
        image = "image_data_with_signature"
        return image
    except Exception as e:
        print(f"Erreur lors de la capture de l'image : {e}")
        return None